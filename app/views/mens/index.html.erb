<% @users.each do |user| %>

<%= image_tag(user.avatar.url , class: "avatar") if user.avatar? %>

<h3> <%= user.username %> </h3>
<div>
    <%= user.sex %>
    <%= user.age %>
    <%= link_to "About He", user_path(user) %>
</div>

<%end%>

<%= render  'edit' %>
<br>
