<% @users.each do |user| %>
<%= image_tag(user.avatar.url , class: "avatar") if user.avatar? %>

<h1> <%= user.username %> </h1>
<div>
    <%= user.sex %>
    <%= user.age%>
    <%= link_to "About She", user_path(user) %>
</div>

<%end%>

<%= will_paginate @users %>
<br>
